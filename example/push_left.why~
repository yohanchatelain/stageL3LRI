theory Push_left 

type Node 'a = Leaf | Node (Node 'a) (Node 'a)

function push_left (x : Node) : Node =
match x with
|Leaf -> Leaf
|Node t Leaf -> Node t Leaf
|Node t1 (Node t2 t3) -> push_left Node (Node t1 t2) t3

end

end